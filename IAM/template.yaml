AWSTemplateFormatVersion: "2010-09-09"
Resources:
  MyUser:
    Type: "AWS::IAM::User"
    Properties:

      UserName: "noel1"
      ManagedPolicyArns:
        - "arn:aws:iam::aws:policy/ReadOnlyAccess"
      LoginProfile:
        # Password: !Sub "${AWS::StackName}-My@246810!"
        # PasswordResetRequired: true
